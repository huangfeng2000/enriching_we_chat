<%= form_for(@customer_point) do |f| %>
  <% if @customer_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_point.errors.count, "error") %> prohibited this customer_point from being saved:</h2>

      <ul>
      <% @customer_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.select :customer_id, customer_codes %>
  </div>
  <div class="field">
    <%= f.label :point %><br>
    <%= f.number_field :point %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
