<%= form_for(@customer) do |f| %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% @customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :we_chat_nickname %><br>
    <%= f.text_field :we_chat_nickname %>
  </div>
  <div class="field">
    <%= f.label :we_chat_openid %><br>
    <%= f.text_field :we_chat_openid %>
  </div>
  <div class="field">
    <%= f.label :tel_no %><br>
    <%= f.text_field :tel_no %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
